---
phase: 02-frontend-uiux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/(dashboard)/media/page.tsx
  - src/components/sidebar.tsx
  - messages/en.json
  - messages/pt-BR.json
  - messages/es.json
autonomous: true
requirements:
  - UI-07
user_setup: []
must_haves:
  truths:
    - "Media Library page no longer exists in the codebase"
    - "Sidebar navigation does not show a Media Library link"
    - "No media.* i18n keys remain in any locale file"
  artifacts:
    - path: "src/app/[locale]/(dashboard)/media/page.tsx"
      provides: "Deleted"
    - path: "src/components/sidebar.tsx"
      provides: "No media navigation item"
    - path: "messages/en.json"
      provides: "No media namespace"
    - path: "messages/pt-BR.json"
      provides: "No media namespace"
    - path: "messages/es.json"
      provides: "No media namespace"
  key_links:
    - from: "src/components/sidebar.tsx"
      to: "messages/*.json"
      via: "t() calls for nav labels"
---

<objective>
Completely remove the Media Library feature from the application.

Purpose: The Media Library was deferred and removed from scope in Phase 4. All code, navigation, and translations must be cleaned up to avoid dead code and confusion.
Output: No Media Library page, navigation, or i18n keys remain.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-uiux/02-CONTEXT.md
@src/components/sidebar.tsx
@messages/en.json
@messages/pt-BR.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete Media Library page and remove from sidebar</name>
  <files>
    src/app/[locale]/(dashboard)/media/page.tsx
    src/components/sidebar.tsx
  </files>
  <action>
    1. Delete the file `src/app/[locale]/(dashboard)/media/page.tsx` entirely.
    2. In `src/components/sidebar.tsx`, remove any navigation item that links to `/media` or references the Media Library. The sidebar uses `linkSubItems` and `socialSubItems` arrays — ensure no entry has `href: "/media"` or similar.
    3. Verify the `media` directory under `src/app/[locale]/(dashboard)/` is empty after page.tsx deletion. If it contains only an empty directory, it can be left (git does not track empty dirs).
    4. Do NOT remove any other unrelated navigation items.
  </action>
  <verify>
    <automated>test ! -f src/app/\[locale\]/\(dashboard\)/media/page.tsx && grep -c 'media' src/components/sidebar.tsx | xargs test 0 -eq</automated>
  </verify>
  <done>Media Library page deleted and sidebar navigation cleaned</done>
</task>

<task type="auto">
  <name>Task 2: Remove media i18n keys from all locale files</name>
  <files>
    messages/en.json
    messages/pt-BR.json
    messages/es.json
  </files>
  <action>
    1. In each of the three locale files, find and remove the entire `"media"` namespace object. It currently contains keys like `"title"`, `"upload"`, `"mediaLibrary"`.
    2. Ensure valid JSON remains after removal (no trailing commas on the preceding object).
    3. Verify no other `"media"` references exist at the top-level namespace level. Other occurrences of the word "media" inside unrelated strings (e.g., "social media") should NOT be removed.
  </action>
  <verify>
    <automated>grep -c '"media":' messages/en.json messages/pt-BR.json messages/es.json | xargs test 0 -eq</automated>
  </verify>
  <done>All media i18n keys removed from en, pt-BR, and es locale files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only removes dead code; no trust boundaries affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | N/A | N/A | accept | No security impact from removing unused code |
</threat_model>

<verification>
- `src/app/[locale]/(dashboard)/media/page.tsx` does not exist
- `grep -r "media" src/components/sidebar.tsx` returns no matches for navigation
- `grep '"media":' messages/*.json` returns no matches
- Build passes: `npm run build` succeeds
</verification>

<success_criteria>
- Media Library page file is deleted
- Sidebar has no Media Library navigation link
- All three locale files have no `media` namespace
- Application builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-uiux/02-01-SUMMARY.md`
</output>
