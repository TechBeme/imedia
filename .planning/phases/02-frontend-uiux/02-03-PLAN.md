---
phase: 02-frontend-uiux
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - messages/en.json
  - messages/pt-BR.json
  - messages/es.json
autonomous: true
requirements:
  - UI-10
user_setup: []
must_haves:
  truths:
    - "All UI text in modified pages is translatable via next-intl"
    - "No hardcoded Portuguese or English strings remain in the modified components"
    - "Application builds successfully with zero TypeScript errors"
    - "All new i18n keys have translations in pt-BR, en, and es"
  artifacts:
    - path: "messages/en.json"
      provides: "Complete translations for all new UI text"
    - path: "messages/pt-BR.json"
      provides: "Complete translations for all new UI text"
    - path: "messages/es.json"
      provides: "Complete translations for all new UI text"
  key_links:
    - from: "src/app/[locale]/(dashboard)/*"
      to: "messages/*.json"
      via: "useTranslations() hooks"
---

<objective>
Audit i18n completeness and verify the build after all UI changes.

Purpose: Ensure no hardcoded strings were introduced and all new UI text is properly translated in all three supported locales. Confirm the application builds cleanly.
Output: Verified i18n coverage and successful build.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-uiux/02-CONTEXT.md
@messages/en.json
@messages/pt-BR.json
@messages/es.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n audit and completion</name>
  <files>
    messages/en.json
    messages/pt-BR.json
    messages/es.json
  </files>
  <action>
    1. Audit all modified pages from plans 02-01 and 02-02 for hardcoded strings:
       - `src/app/[locale]/(dashboard)/scheduled/page.tsx`
       - `src/app/[locale]/(dashboard)/history/page.tsx`
       - `src/app/[locale]/(dashboard)/analytics/page.tsx`
       - `src/app/[locale]/(dashboard)/dashboard/dashboard-content.tsx`
       - `src/components/sidebar.tsx`
    2. For any hardcoded string found (text inside JSX that is not wrapped in `t()`), either:
       - Wrap it in `useTranslations` if it's user-facing
       - Or leave it if it's a technical value (e.g., "#00001", date formats)
    3. Ensure all new i18n keys added in plans 02-01 and 02-02 exist in ALL three locale files with proper translations:
       - pt-BR (Portuguese, default)
       - en (English)
       - es (Spanish)
    4. Verify JSON validity of all three locale files:
       - `node -e "JSON.parse(require('fs').readFileSync('messages/en.json'))"`
       - Same for pt-BR.json and es.json
    5. Check for missing keys across locales: any key in pt-BR must exist in en and es, and vice versa.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); ['en','pt-BR','es'].forEach(l=>JSON.parse(fs.readFileSync('messages/'+l+'.json')))" && echo "JSON valid"</automated>
  </verify>
  <done>All UI text is translatable, all locales have matching keys, JSON is valid</done>
</task>

<task type="auto">
  <name>Task 2: Build verification and final commit</name>
  <files>
    - (no file modifications — verification only)
  </files>
  <action>
    1. Run the TypeScript compiler to check for errors:
       - `npx tsc --noEmit` or `npm run typecheck` if available
    2. Run the Next.js build:
       - `npm run build`
    3. If any errors exist, fix them (likely related to the removed media page or new i18n keys).
    4. Run a quick grep to ensure no "media" references remain in the dashboard pages or sidebar.
    5. Commit all changes with a descriptive message.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -c "error" | xargs test 0 -eq</automated>
  </verify>
  <done>TypeScript compiles without errors, Next.js build succeeds, all changes committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan; no trust boundaries affected |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | N/A | N/A | accept | No security impact from verification |
</threat_model>

<verification>
- `npm run build` succeeds with zero errors
- All three locale files are valid JSON
- No hardcoded user-facing strings in modified components
- No "media" references remain in dashboard code
</verification>

<success_criteria>
- TypeScript compilation passes
- Next.js build succeeds
- All new i18n keys exist in pt-BR, en, and es
- No hardcoded UI strings in modified files
- Git commit created with all phase 2 changes
</success_criteria>

<output>
After completion, create `.planning/phases/02-frontend-uiux/02-03-SUMMARY.md`
</output>
