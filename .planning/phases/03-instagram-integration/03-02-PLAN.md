---
phase: 03-instagram-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/instagram/media/route.ts
  - src/app/api/instagram/account/route.ts
  - src/app/[locale]/(dashboard)/accounts/page.tsx
  - messages/en.json
  - messages/es.json
  - messages/pt-BR.json
autonomous: true
requirements:
  - IG-02
  - IG-07
must_haves:
  truths:
    - User sees Instagram profile info (username, followers, bio, profile pic) on accounts page
    - User sees published posts grid with media preview, likes, and comments
    - Data refreshes automatically when account is connected
    - All UI text is translatable via next-intl
  artifacts:
    - path: src/app/api/instagram/media/route.ts
      provides: Profile + media fetch with token validation
      exports: ["GET"]
    - path: src/app/api/instagram/account/route.ts
      provides: Dedicated account info endpoint
      exports: ["GET"]
    - path: src/app/[locale]/(dashboard)/accounts/page.tsx
      provides: Instagram profile display and posts grid
      exports: ["default"]
    - path: messages/pt-BR.json
      provides: Portuguese translations for accounts page
      contains: "accounts" namespace
  key_links:
    - from: accounts/page.tsx
      to: /api/instagram/media
      via: fetch in useEffect
      pattern: "fetch.*api/instagram/media"
    - from: accounts/page.tsx
      to: /api/instagram/account
      via: fetch in useEffect
      pattern: "fetch.*api/instagram/account"
    - from: api/instagram/media
      to: socialAccounts table
      via: db.select with decrypted token
      pattern: "decrypt.*accessToken"
---

<objective>
Enhance the Instagram account info display and media listing to show rich profile data (bio, followers, profile picture) and published posts with engagement metrics. Ensure all UI text is translatable.

Purpose: The accounts page already has the UI structure for displaying Instagram profile and posts. The API already tries Graph API first for rich data. This plan ensures the data flows correctly, handles errors gracefully, and all text is i18n-ready.

Output: Fully functional accounts page showing Instagram profile and posts with proper i18n.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

## Current State

The accounts page (src/app/[locale]/(dashboard)/accounts/page.tsx) already:
- Fetches /api/social-accounts for connection status
- Fetches /api/instagram/media for profile + posts
- Displays profile header with avatar, username, stats (posts, followers, following), bio, website
- Displays posts grid with hover overlay showing likes/comments
- Has connect/disconnect buttons
- Uses hardcoded Portuguese strings in some places

The media API (src/app/api/instagram/media/route.ts) already:
- Tries graph.facebook.com first for rich Business account data
- Falls back to graph.instagram.com
- Returns profile + media + debug info

## InstagramMedia Interface (from media/route.ts)
```typescript
export interface InstagramMedia {
    id: string;
    caption: string | null;
    media_type: "IMAGE" | "VIDEO" | "CAROUSEL_ALBUM";
    media_url: string;
    thumbnail_url?: string;
    permalink: string;
    timestamp: string;
    like_count: number;
    comments_count: number;
}
```

## InstagramProfile Interface (from accounts/page.tsx)
```typescript
interface InstagramProfile {
    username: string | null;
    name: string | null;
    mediaCount: number;
    followersCount: number;
    followsCount: number;
    biography: string;
    website: string;
    profilePictureUrl: string | null;
}
```

## i18n Pattern
```typescript
const t = useTranslations("accounts");
const tc = useTranslations("common");
```

Messages files are at messages/en.json, messages/es.json, messages/pt-BR.json.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor media API to use shared token helper and add account endpoint</name>
  <files>src/app/api/instagram/media/route.ts, src/app/api/instagram/account/route.ts</files>
  <action>
    1. Update src/app/api/instagram/media/route.ts:
       - Import and use getValidInstagramToken from src/lib/instagram.ts (created in Plan 01)
       - Remove inline token decryption logic; use the shared helper
       - Keep the Graph API-first + fallback logic for profile and media
       - Remove debug field from response (was for development only)
       - Ensure response format: { media: InstagramMedia[], profile: InstagramProfile }
    
    2. Create src/app/api/instagram/account/route.ts:
       - GET endpoint that returns only profile info (no media)
       - Uses getValidInstagramToken for token management
       - Same Graph API-first + fallback logic as media endpoint
       - Response: { profile: InstagramProfile }
       - Protected by auth and rate limiting
    
    3. Both endpoints must handle token expiry gracefully — if token is expired and refresh fails, return error prompting reconnection
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/imedia && npx tsc --noEmit 2>&1 | grep -c "instagram/media\|instagram/account" || echo "0 type errors"</automated>
  </verify>
  <done>
    - media/route.ts uses shared getValidInstagramToken helper
    - account/route.ts exists and returns profile data
    - Both endpoints handle expired tokens gracefully
    - Debug field removed from media response
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add i18n strings and fix hardcoded text in accounts page</name>
  <files>messages/en.json, messages/es.json, messages/pt-BR.json, src/app/[locale]/(dashboard)/accounts/page.tsx</files>
  <action>
    1. Audit accounts/page.tsx for ALL hardcoded strings:
       - "Plataformas" → t("platforms")
       - "Conecte seu Instagram" → t("connectTitle")
       - "Conecte sua conta do Instagram..." → t("connectDescription")
       - "Conectar Instagram" → t("connectButton")
       - "Conectando..." → t("connecting")
       - "Desconectar conta" → t("disconnectButton")
       - "posts" → t("posts")
       - "seguidores" → t("followers")
       - "seguindo" → t("following")
       - "Publicacoes" → t("publications")
       - "Nenhuma publicacao encontrada" → t("noPublications")
       - "Erro ao carregar dados" → t("loadError")
       - "Tente reconectar sua conta" → t("reconnectPrompt")
       - "Popup bloqueado..." → t("popupBlocked")
       - "Edicao de posts em breve!" → t("editComingSoon")
    
    2. Add all keys to messages/pt-BR.json under "accounts" namespace
    3. Add translations to messages/en.json and messages/es.json
    4. Update page.tsx to use t() for all user-facing strings
    5. Keep console.log/debug strings as-is (not user-facing)
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/imedia && grep -n "Conecte\|Conectar\|Publicacoes\|seguidores\|seguindo\|Plataformas" src/app/\[locale\]/\(dashboard\)/accounts/page.tsx | wc -l | xargs -I {} test {} -eq 0 && echo "No hardcoded strings" || echo "Hardcoded strings remain"</automated>
  </verify>
  <done>
    - All user-facing strings in accounts/page.tsx use t() or tc()
    - Translations exist in all 3 locale files
    - No hardcoded Portuguese/English/Spanish strings remain in UI
    - Page still renders correctly
  </done>
</task>

<task type="auto">
  <name>Task 3: Add loading states, error handling, and empty states</name>
  <files>src/app/[locale]/(dashboard)/accounts/page.tsx</files>
  <action>
    1. Add skeleton loader for profile section while loading:
       - Use Skeleton component from @/components/ui/skeleton
       - Show shimmer for avatar, username, stats, bio
    
    2. Add skeleton grid for posts while loading:
       - 6 skeleton cards in 3-column grid
    
    3. Improve error state:
       - When apiError is set, show inline error banner (already exists)
       - Add "Reconectar" button in error banner that triggers handleConnect
       - Clear error state on successful reconnect
    
    4. Improve empty state for posts:
       - Keep existing icon + text
       - Add subtle CTA: "Crie seu primeiro post na pagina Compose" (translatable)
    
    5. Add retry logic for fetchInstagramMedia:
       - If fetch fails, allow user to click "Tentar novamente" (translatable)
    
    6. Ensure all new strings are added to i18n files
  </action>
  <verify>
    <automated>cd /home/ubuntu/work/imedia && npx tsc --noEmit 2>&1 | grep -c "accounts/page" || echo "0 type errors"</automated>
  </verify>
  <done>
    - Skeleton loaders visible during initial load
    - Error banner includes reconnect button
    - Empty state has CTA to compose page
    - Retry button available on fetch failure
    - All new strings are translatable
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Media URLs from Instagram displayed in img tags; must be HTTPS |
| API → Meta | Access tokens sent to Meta APIs; must be valid and fresh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Profile data exposed to wrong user | mitigate | API filters by session.user.id; no userId param in request |
| T-03-06 | Denial of Service | Large media grid causes slow loading | accept | Limit to 18 items; lazy loading on images already present |
| T-03-07 | Spoofing | Fake Instagram profile data injected | accept | Data comes directly from Meta APIs; no user input into profile display |
</threat_model>

<verification>
- Accounts page loads without errors
- Profile data displays correctly when connected
- Posts grid renders with images and engagement counts
- All text is translatable (switch locale to verify)
- Loading skeletons appear during fetch
- Error state shows reconnect button
</verification>

<success_criteria>
- Instagram profile shows username, followers, bio, profile picture
- Posts grid shows media with likes and comments on hover
- All UI text is translatable via next-intl
- Loading and error states are polished
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-instagram-integration/03-02-SUMMARY.md`
</output>
