---
phase: 04-scheduled-posts
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/app/[locale]/(dashboard)/compose/page.tsx
  - src/app/[locale]/(dashboard)/scheduled/page.tsx
  - messages/en.json
  - messages/pt-BR.json
  - messages/es.json
autonomous: true
requirements:
  - SCH-03
  - SCH-04
must_haves:
  truths:
    - Compose page has a toggle between "Publish Now" and "Schedule"
    - When scheduling, a date/time picker is shown
    - Scheduled time must be in the future
    - On schedule, POST to /api/scheduled-posts is called instead of /api/instagram/publish
    - /scheduled page shows real data from the API
    - Each scheduled post shows: content preview, platform icon, scheduled time, status badge
    - Actions available: Edit, Cancel, Publish Now
    - Filters by status: All, Pending, Published, Failed, Cancelled
  artifacts:
    - path: "src/app/[locale]/(dashboard)/compose/page.tsx"
      provides: "Compose with scheduling toggle"
    - path: "src/app/[locale]/(dashboard)/scheduled/page.tsx"
      provides: "Scheduled posts dashboard"
      min_lines: 150
  key_links:
    - from: "compose/page.tsx"
      to: "/api/scheduled-posts"
      via: "fetch POST on schedule"
    - from: "scheduled/page.tsx"
      to: "/api/scheduled-posts"
      via: "fetch GET on mount"
---

<objective>
Integrate scheduling into the compose page and build the real scheduled posts dashboard.

Purpose: User can schedule posts from compose and manage them on the scheduled page.
Output: Functional compose scheduling + real-time scheduled posts dashboard.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-scheduled-posts/04-CONTEXT.md
@src/app/[locale]/(dashboard)/compose/page.tsx
@src/app/[locale]/(dashboard)/scheduled/page.tsx
@src/components/ui/calendar.tsx
@src/components/ui/popover.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scheduling toggle to Compose page</name>
  <files>
    src/app/[locale]/(dashboard)/compose/page.tsx
    messages/en.json
    messages/pt-BR.json
    messages/es.json
  </files>
  <action>
    Modify the compose page to support scheduling:

    1. Add state: `isScheduling` (boolean), `scheduledAt` (Date | null)
    2. Add a toggle switch: "Publicar agora" / "Agendar" (Publish Now / Schedule)
    3. When `isScheduling` is true, show a date + time picker:
       - Use shadcn/ui Calendar + Popover for date
       - Use a time input (HTML5 `<input type="time">`) for time
       - Combine into a single Date object
       - Validate: must be at least 5 minutes in the future
    4. Modify `handlePublish`:
       - If `isScheduling` is false: keep existing behavior (POST to /api/instagram/publish)
       - If `isScheduling` is true: POST to /api/scheduled-posts with `{ platform: "instagram", content, mediaUrls, scheduledAt }`
       - On success: toast "Agendado com sucesso!" / "Scheduled successfully!"
    5. Only show scheduling toggle when Instagram is the only selected platform (since only Instagram publishing is implemented). If other platforms are selected, disable scheduling with a tooltip explaining.
    6. Add i18n keys for scheduling under the "compose" namespace:
       - `schedule`, `schedulePost`, `publishNow`, `selectDateTime`, `scheduleErrorPast`, `scheduledSuccess`

    Do NOT break the existing immediate publish flow. The default should remain "Publish Now".
  </action>
  <verify>
    <automated>grep -c "isScheduling\|scheduledAt" src/app/\[locale\]/\(dashboard\)/compose/page.tsx | xargs test 2 -le</automated>
  </verify>
  <done>Compose page has scheduling toggle, date/time picker, and posts to /api/scheduled-posts when scheduling. Immediate publish still works.</done>
</task>

<task type="auto">
  <name>Task 2: Build real Scheduled Posts dashboard</name>
  <files>
    src/app/[locale]/(dashboard)/scheduled/page.tsx
    messages/en.json
    messages/pt-BR.json
    messages/es.json
  </files>
  <action>
    Rewrite `src/app/[locale]/(dashboard)/scheduled/page.tsx` to use real data:

    1. Fetch scheduled posts on mount via `GET /api/scheduled-posts`
    2. State: `posts[]`, `filter` (all | pending | published | failed | cancelled), `loading`
    3. Display as a list (keep the existing list view, remove the calendar view placeholder or keep it as a future enhancement):
       - Each row/card shows:
         - Platform icon (Instagram = pink, etc.)
         - Content preview (truncated to 100 chars)
         - Scheduled date/time formatted with date-fns
         - Status badge: `pending` (yellow), `published` (green), `failed` (red), `cancelled` (gray)
         - If failed, show error message tooltip
       - Actions per post (only for pending):
         - "Edit" button → opens a modal with content + date/time editable
         - "Cancel" button → calls DELETE /api/scheduled-posts?id={id}
         - "Publish Now" button → calls a new endpoint or reuses logic to publish immediately
    4. Filter tabs at the top: All, Pending, Published, Failed, Cancelled
    5. Empty state when no posts match the filter
    6. Loading skeleton while fetching
    7. Auto-refresh every 30 seconds (for status updates from cron)
    8. Add i18n keys under "scheduled" namespace:
       - `title`, `all`, `pending`, `published`, `failed`, `cancelled`, `edit`, `cancel`, `publishNow`, `noPosts`, `scheduledFor`, `contentPreview`

    Use the existing layout and styling patterns (glass-card, motion animations).
  </action>
  <verify>
    <automated>grep -c "fetch.*scheduled-posts\|useEffect" src/app/\[locale\]/\(dashboard\)/scheduled/page.tsx | xargs test 1 -le</automated>
  </verify>
  <done>Scheduled page fetches real data, shows posts with status badges, supports filtering, and has edit/cancel/publish-now actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | Untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Edit modal | mitigate | API already validates ownership and status on PUT |
| T-04-05 | Information Disclosure | Scheduled list | mitigate | API only returns posts for authenticated user |
</threat_model>

<verification>
- Scheduling toggle appears only when Instagram is selected
- Date/time in the past is rejected with error toast
- Scheduled post appears on /scheduled within seconds
- Filter tabs correctly filter by status
- Edit modal pre-fills content and date/time
- Cancel updates status to cancelled
- Publish Now triggers immediate publish and updates status
</verification>

<success_criteria>
- User can schedule a post from compose with future date/time
- Scheduled posts appear on /scheduled with correct status
- User can edit, cancel, or publish-now pending posts
- All UI text is i18n-ready
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04-scheduled-posts/04-02-SUMMARY.md`
</output>
