---
phase: 2.1-link-shortener
plan: 05
type: execute
wave: 3
depends_on:
  - 02.1-01-PLAN.md
  - 02.1-02-PLAN.md
  - 02.1-03-PLAN.md
  - 02.1-04-PLAN.md
files_modified:
  - src/components/sidebar.tsx
  - src/messages/en.json
  - src/messages/es.json
  - src/messages/pt-BR.json
  - drizzle/
  - next.config.ts
autonomous: true
requirements:
  - LNK-01
  - LNK-02
  - LNK-03
  - LNK-04
  - LNK-05
  - LNK-06
  - LNK-07
  - LNK-08
  - LNK-09
  - LNK-10
  - LNK-11
  - LNK-12
user_setup: []
must_haves:
  truths:
    - "Links navigation item appears in sidebar"
    - "All database migrations are generated and applied"
    - "Application builds successfully with all new code"
    - "All i18n strings are present in 3 locales"
    - "Public shorten page is accessible without auth"
  artifacts:
    - path: "src/components/sidebar.tsx"
      provides: "Navigation with Links item"
      contains: "links"
    - path: "drizzle/"
      provides: "Migration files for new tables"
  key_links:
    - from: "src/components/sidebar.tsx"
      to: "/[locale]/links"
      via: "Navigation link"
      pattern: "href=.*links"
---

<objective>
Integrate the link shortener into the app navigation and finalize the phase.

Purpose: Users need to discover the link shortener feature from the main dashboard, and the database schema must be pushed to production.
Output: Updated sidebar with Links navigation, generated migrations, and a working build.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/sidebar.tsx
@src/messages/pt-BR.json

<interfaces>
From src/components/sidebar.tsx:
```typescript
// Sidebar with navigation items using next-intl Link
// Current items: dashboard, accounts, compose, scheduled, history, analytics, media, settings
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Links to sidebar navigation</name>
  <files>src/components/sidebar.tsx, messages/en.json, messages/es.json, messages/pt-BR.json</files>
  <read_first>
    - src/components/sidebar.tsx (current navigation items)
    - messages/pt-BR.json (nav section)
  </read_first>
  <action>
    Update src/components/sidebar.tsx:
    - Add "links" navigation item between "media" and "settings" (or in a logical position)
    - Use the same pattern as existing nav items: icon + label + href
    - Icon suggestion: use a link/chain icon from react-icons (e.g., FiLink from react-icons/fi or similar)
    - href: `/${locale}/links`
    - Label: use i18n key `nav.links`

    Ensure messages files have the nav.links key (already added in Plan 02):
    - pt-BR: "Links"
    - en: "Links"
    - es: "Enlaces"

    Also add nav.links to the nav section of all 3 message files if not already present.
  </action>
  <verify>
    <automated>grep -q "nav.links" messages/pt-BR.json && grep -q "links" src/components/sidebar.tsx && echo "Sidebar OK"</automated>
  </verify>
  <done>
    - Sidebar contains Links navigation item
    - Links item uses i18n key nav.links
    - Links item appears in correct position
    - All 3 locale files have nav.links translation
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Generate and push database migrations</name>
  <files>drizzle/</files>
  <read_first>
    - drizzle.config.ts
    - src/db/schema.ts (after all Plan changes)
    - package.json (scripts)
  </read_first>
  <action>
    Run the following commands in sequence:
    1. `npm run db:generate` — Generate Drizzle migration for new tables (shortLinks, linkClicks, customDomains)
    2. `npm run db:push` — Push migration to the database
    3. Verify migration files exist in drizzle/ directory

    If db:push fails due to interactive prompts, use:
    - `npx drizzle-kit push --force` or handle the prompt

    This is a BLOCKING task — the phase cannot be considered complete without the schema being applied to the database.
  </action>
  <verify>
    <automated>ls drizzle/*.sql | grep -q "sql" && echo "Migration generated"</automated>
  </verify>
  <done>
    - Migration files generated in drizzle/ directory
    - Migration pushed to database successfully
    - All new tables exist in the database
  </done>
</task>

<task type="auto">
  <name>Task 3: Final build verification and integration test</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts
    - src/app/[locale]/(dashboard)/layout.tsx
  </read_first>
  <action>
    1. Run `npm run build` to verify the entire application builds without errors
    2. Run `npx tsc --noEmit` to verify no TypeScript errors
    3. Verify next.config.ts has no conflicts with new routes
    4. Check that the public shorten page route (/[locale]/s) does not conflict with existing routes
    5. Verify that /[slug] dynamic route does not conflict with existing static routes (login, register, etc.)
       - The [slug] route should be at the root level, not under [locale]
       - This means short links work as /abc123 not /pt-BR/abc123
       - Update if needed: move src/app/[slug]/route.ts to the correct location

    If there are route conflicts:
    - The [slug] route at root level may conflict with [locale] for 2-3 character slugs
    - Solution: Use a longer default slug length (7-8 chars) to avoid collision with locale codes
    - Or: Keep [slug] under [locale] but that makes short URLs longer
    - Decision: Keep [slug] at root level; default slug length should be 7+ chars

    Update src/lib/links.ts if needed to generate longer default slugs (7 chars instead of 6).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -5 | grep -q "Build succeeded" && echo "Build OK"</automated>
  </verify>
  <done>
    - `npm run build` completes successfully
    - `npx tsc --noEmit` passes
    - No route conflicts between [slug] and existing pages
    - Default slug length avoids locale collision (7+ chars)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build -> Deployment | Schema must be pushed before deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-19 | Denial of Service | Route collision | mitigate | Default slug length 7+ chars avoids collision with 2-char locale codes |
| T-2.1-20 | Information Disclosure | Public page exposure | accept | Public shorten page is intentionally public; no sensitive data exposed |
</threat_model>

<verification>
- Run `npm run build` successfully
- Run `npx tsc --noEmit` successfully
- Run `npm run db:generate` and `npm run db:push`
- Verify sidebar shows Links item
- Verify /[locale]/links page loads
- Verify /[locale]/s page loads
- Verify /[slug] redirects work
</verification>

<success_criteria>
- Sidebar navigation includes Links item
- Database migrations generated and applied
- Application builds without errors
- No route conflicts
- All i18n strings present in 3 locales
- Phase is ready for execution
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-link-shortener/02.1-05-SUMMARY.md`
</output>
