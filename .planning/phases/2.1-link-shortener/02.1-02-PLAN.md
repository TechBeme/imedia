---
phase: 2.1-link-shortener
plan: 02
type: execute
wave: 2
depends_on:
  - 02.1-01-PLAN.md
files_modified:
  - src/app/[locale]/(dashboard)/links/page.tsx
  - src/app/[locale]/(dashboard)/links/[id]/page.tsx
  - src/app/[locale]/s/page.tsx
  - src/components/link-card.tsx
  - src/components/link-form.tsx
  - src/components/link-password-form.tsx
  - src/app/api/links/route.ts
  - src/app/api/links/[id]/route.ts
  - messages/en.json
  - messages/es.json
  - messages/pt-BR.json
autonomous: true
requirements:
  - LNK-07
  - LNK-08
  - LNK-09
  - LNK-11
user_setup: []
must_haves:
  truths:
    - "User can view a list of all their short links"
    - "User can copy, edit, delete, and toggle active/inactive on links"
    - "Anonymous users can create short links on a public page"
    - "Password-protected links show a password prompt before redirecting"
    - "Expired links show an expiration message"
  artifacts:
    - path: "src/app/[locale]/(dashboard)/links/page.tsx"
      provides: "Link management dashboard listing user's links"
    - path: "src/app/[locale]/(dashboard)/links/[id]/page.tsx"
      provides: "Link detail/edit page"
    - path: "src/app/[locale]/s/page.tsx"
      provides: "Public shorten page for anonymous users"
    - path: "src/components/link-card.tsx"
      provides: "Reusable link card component"
    - path: "src/components/link-form.tsx"
      provides: "Form for creating/editing links"
    - path: "src/app/api/links/[id]/route.ts"
      provides: "GET/PUT/DELETE for individual links"
  key_links:
    - from: "src/app/[locale]/(dashboard)/links/page.tsx"
      to: "/api/links"
      via: "fetch GET for list"
      pattern: "fetch.*api/links"
    - from: "src/components/link-card.tsx"
      to: "/api/links/[id]"
      via: "fetch DELETE/PUT for actions"
      pattern: "fetch.*api/links/"
---

<objective>
Build the UI for link management and the public shorten page.

Purpose: Users need a dashboard to manage their links and a public page for anonymous link creation.
Output: Dashboard links page, link detail/edit page, public shorten page, and supporting API endpoints.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/[locale]/(dashboard)/layout.tsx
@src/components/ui/
@messages/pt-BR.json

<interfaces>
From src/app/[locale]/(dashboard)/layout.tsx:
```typescript
// Dashboard layout with Sidebar + Header, auth-protected
export default async function DashboardLayout({ children, params });
```

From src/db/schema.ts (shortLinks table from Plan 01):
```typescript
export const shortLinks = pgTable("short_links", {
    id: uuid("id").defaultRandom().primaryKey(),
    userId: text("user_id").references(() => user.id, { onDelete: "cascade" }),
    originalUrl: text("original_url").notNull(),
    slug: text("slug").notNull().unique(),
    customSlug: boolean("custom_slug").notNull().default(false),
    domain: text("domain").notNull().default(""),
    password: text("password"),
    expiresAt: timestamp("expires_at"),
    isActive: boolean("is_active").notNull().default(true),
    clickCount: integer("click_count").notNull().default(0),
    createdAt: timestamp("created_at").notNull().defaultNow(),
    updatedAt: timestamp("updated_at").notNull().defaultNow(),
});
```

Existing UI components available: card, button, input, dialog, dropdown-menu, badge, tabs, table, tooltip, sonner (toast), skeleton, switch, label, textarea, select, popover, calendar.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API with GET list, PUT, DELETE for links</name>
  <files>src/app/api/links/route.ts, src/app/api/links/[id]/route.ts</files>
  <read_first>
    - src/app/api/links/route.ts (from Plan 01)
    - src/app/api/platform-credentials/route.ts (GET list pattern)
    - src/lib/api-response.ts
    - src/lib/session.ts
    - src/db/schema.ts
  </read_first>
  <action>
    Extend src/app/api/links/route.ts:
    - Add GET handler: query shortLinks where userId matches session user, order by createdAt desc. Return success({ links: [] })
    - If no session: return unauthorized()

    Create src/app/api/links/[id]/route.ts:
    - GET /api/links/[id]: fetch single link by id where userId matches session. Return success({ link }) or notFound()
    - PUT /api/links/[id]: update link fields (originalUrl, isActive, password, expiresAt). Validate with Zod. Only owner can update.
    - DELETE /api/links/[id]: delete link where userId matches session. Return success({ deleted: true }) or notFound()
    - All handlers require authenticated session
  </action>
  <verify>
    <automated>curl -X GET http://localhost:3000/api/links -H "Cookie: session=..." | grep -q "links" && echo "GET list OK"</automated>
  </verify>
  <done>
    - GET /api/links returns list of user's links
    - GET /api/links/[id] returns single link
    - PUT /api/links/[id] updates link fields
    - DELETE /api/links/[id] deletes link
    - All endpoints require authentication and validate ownership
  </done>
</task>

<task type="auto">
  <name>Task 2: Build link management dashboard page</name>
  <files>src/app/[locale]/(dashboard)/links/page.tsx, src/components/link-card.tsx, src/components/link-form.tsx</files>
  <read_first>
    - src/app/[locale]/(dashboard)/accounts/page.tsx (existing dashboard page pattern)
    - src/components/ui/card.tsx, button.tsx, badge.tsx, dialog.tsx, switch.tsx
    - src/app/[locale]/(dashboard)/layout.tsx
    - messages/pt-BR.json (for i18n keys)
  </read_first>
  <action>
    Create src/app/[locale]/(dashboard)/links/page.tsx:
    - Server component that fetches links via fetch to /api/links (or direct db query with auth check)
    - Display page title "Links" with i18n
    - Show create link button that opens a Dialog with LinkForm
    - Render links in a responsive grid or table using LinkCard components
    - Empty state: illustration + "No links yet" message + create button
    - Loading state: skeleton cards

    Create src/components/link-card.tsx:
    - Props: link object with id, slug, originalUrl, clickCount, isActive, createdAt, customSlug, expiresAt
    - Display: short URL (with copy button), original URL (truncated), click count badge, created date
    - Actions: copy short URL, toggle active/inactive (switch), edit (opens dialog with LinkForm), delete (confirm dialog), view analytics (link to /links/[id])
    - Status badge: Active/Inactive/Expired
    - Use Card, Button, Badge, Switch, Dialog from shadcn/ui
    - Toast notifications for copy success, toggle success, delete success

    Create src/components/link-form.tsx:
    - Props: mode: "create" | "edit", initialData?, onSuccess?
    - Form fields: originalUrl (input, required), customSlug (input, optional), password (input, optional), expiresAt (date picker, optional)
    - Validation: Zod schema — originalUrl must be valid URL, customSlug must match /^[a-zA-Z0-9_-]{3,50}$/ if provided
    - Submit: POST /api/links for create, PUT /api/links/[id] for edit
    - Show loading state during submit
    - Error handling: display API error messages
    - Use React Hook Form + Zod resolver

    Add i18n keys to messages/pt-BR.json, messages/en.json, messages/es.json:
    - nav.links: "Links" / "Links" / "Enlaces"
    - links.title: "Meus Links" / "My Links" / "Mis Enlaces"
    - links.create: "Criar Link" / "Create Link" / "Crear Enlace"
    - links.originalUrl: "URL Original" / "Original URL" / "URL Original"
    - links.shortUrl: "URL Curta" / "Short URL" / "URL Corta"
    - links.clicks: "Cliques" / "Clicks" / "Clics"
    - links.copy: "Copiar" / "Copy" / "Copiar"
    - links.edit: "Editar" / "Edit" / "Editar"
    - links.delete: "Excluir" / "Delete" / "Eliminar"
    - links.deleteConfirm: "Tem certeza?" / "Are you sure?" / "Estas seguro?"
    - links.active: "Ativo" / "Active" / "Activo"
    - links.inactive: "Inativo" / "Inactive" / "Inactivo"
    - links.expired: "Expirado" / "Expired" / "Expirado"
    - links.noLinks: "Nenhum link criado ainda" / "No links created yet" / "Aun no hay enlaces"
    - links.createFirst: "Crie seu primeiro link" / "Create your first link" / "Crea tu primer enlace"
    - links.customSlug: "Slug personalizado" / "Custom slug" / "Slug personalizado"
    - links.password: "Senha de protecao" / "Password protection" / "Proteccion con contrasena"
    - links.expiresAt: "Data de expiracao" / "Expiration date" / "Fecha de expiracion"
    - links.optional: "Opcional" / "Optional" / "Opcional"
  </action>
  <verify>
    <automated>grep -q "links.title" messages/pt-BR.json && grep -q "links.title" messages/en.json && grep -q "links.title" messages/es.json && echo "i18n OK"</automated>
  </verify>
  <done>
    - Dashboard links page renders at /[locale]/links
    - LinkCard displays link info with copy, toggle, edit, delete actions
    - LinkForm creates and edits links with validation
    - All text is i18n-ready in 3 locales
    - Empty and loading states handled
    - Toast notifications for user feedback
  </done>
</task>

<task type="auto">
  <name>Task 3: Build public shorten page and password prompt</name>
  <files>src/app/[locale]/s/page.tsx, src/app/[slug]/route.ts (update)</files>
  <read_first>
    - src/app/[locale]/login/page.tsx (public page pattern)
    - src/components/link-form.tsx (from Task 2)
    - src/app/[slug]/route.ts (from Plan 01)
    - src/app/[locale]/layout.tsx
  </read_first>
  <action>
    Create src/app/[locale]/s/page.tsx (public shorten page):
    - Public page — no auth required
    - Simple, clean design with large input for original URL
    - Optional fields: custom slug, password, expiration (collapsible advanced options)
    - Submit to POST /api/links (no session = anonymous link, not saved to account)
    - On success: display the short URL with copy button and QR code preview
    - Recent links section: store recently created links in localStorage, display them
    - Use the same LinkForm component but adapted for public use (no auth requirement)
    - Responsive design, mobile-friendly

    Update src/app/[slug]/route.ts (from Plan 01) for password prompt:
    - When link has password, render a simple HTML page (not React) with:
      - Title: "Link protegido" / "Protected link"
      - Input field for password
      - Submit button
      - On correct password: redirect to originalUrl
      - On wrong password: show error message
    - Use a simple inline HTML template (no external dependencies) for the password page
    - The password form POSTs to the same URL with form data; the route handler checks for POST method
  </action>
  <verify>
    <automated>curl -s http://localhost:3000/pt-BR/s | grep -q "URL" && echo "Public page OK"</automated>
  </verify>
  <done>
    - Public shorten page at /[locale]/s works without authentication
    - Creates anonymous short links
    - Displays short URL and copy button on success
    - Recent links stored in localStorage
    - Password-protected links show a simple password prompt page
    - Wrong password shows error; correct password redirects
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Dashboard | Authenticated users only for link management |
| Client -> Public Page | Anonymous users can create links |
| Password Form -> Redirect | Password verification before external redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2.1-08 | Information Disclosure | Link list API | mitigate | GET /api/links only returns links where userId matches session; no access to other users' links |
| T-2.1-09 | Elevation of Privilege | Link edit/delete | mitigate | PUT/DELETE /api/links/[id] validates ownership via userId before allowing mutation |
| T-2.1-10 | Denial of Service | Public shorten page | mitigate | Rate limit 10 req/min per IP (same as API) |
| T-2.1-11 | Information Disclosure | Password prompt | mitigate | Password form uses POST (not GET) to avoid leaking password in URL/query params |
</threat_model>

<verification>
- Run `npm run build` to verify no build errors
- Run `npx tsc --noEmit` to verify no TypeScript errors
- Verify /[locale]/links page loads and shows link list
- Verify /[locale]/s page works for anonymous link creation
- Verify password-protected link shows prompt and validates correctly
- Verify i18n strings appear in all 3 locales
</verification>

<success_criteria>
- Dashboard links page lists all user's links with actions
- LinkCard component is reusable and shows all link info
- LinkForm creates and edits links with validation
- Public shorten page works for anonymous users
- Password-protected links require correct password before redirect
- All text is translated in pt-BR, en, es
- Build and TypeScript checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/2.1-link-shortener/02.1-02-SUMMARY.md`
</output>
